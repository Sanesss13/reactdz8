import { TOGGLE_SHOW_NAME_ACTION } from "../constants";
import { profileReducer } from "../reducer";

describe("test profile reducer", () => {
  test('test toggleShowNameAction', () => {
    const action = {
      type: TOGGLE_SHOW_NAME_ACTION,
    }
  });
});
